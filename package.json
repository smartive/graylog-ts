{
  "name": "graylog-ts",
  "version": "0.1.4",
  "description": "Graylog implementation for typescript",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npm install -s && typings install",
    "clean": "rimraf build",
    "develop": "npm run clean && tsc --sourceMap -w --pretty",
    "build": "npm run clean && tsc --outDir .",
    "postversion": "git push && git push --tags",
    "test": "echo 'no tests defined'"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:smartive/graylog-ts.git"
  },
  "keywords": [
    "graylog",
    "typescript",
    "logging"
  ],
  "author": "Christoph Bühler <christoph@smartive.ch>",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^2.5.2",
    "tslint": "^3.10.2",
    "typescript": "^1.8.10",
    "typings": "^1.0.4"
  },
  "dependencies": {
    "graylog2": "^0.1.3",
    "rxjs": "^5.0.0-beta.8"
  }
}
